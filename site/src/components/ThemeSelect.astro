---
/**
 * Starlight `ThemeSelect` override: renders nothing.
 *
 * The site is dark-only (see ThemeProvider.astro), so a light/dark toggle in
 * the header would be a lie — it gets removed instead.
 */
---
