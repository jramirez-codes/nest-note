{
  "name": "ainotepad-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Landing page and documentation for ainotepad. Independent sub-project — not part of the React Native app's dependency graph.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "gen:theme": "node scripts/gen-theme-css.mjs",
    "check:theme": "npm run gen:theme && git ls-files --error-unmatch src/styles/catppuccin.generated.css > /dev/null && git diff --exit-code -- src/styles/catppuccin.generated.css",
    "predev": "npm run gen:theme",
    "prebuild": "npm run gen:theme",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "0.41.3",
    "astro": "7.1.1",
    "sharp": "0.35.3"
  },
  "devDependencies": {
    "starlight-links-validator": "0.25.2"
  }
}
